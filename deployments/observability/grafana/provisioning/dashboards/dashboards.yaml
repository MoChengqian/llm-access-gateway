apiVersion: 1

providers:
  - name: llm-access-gateway
    folder: LLM Access Gateway
    type: file
    disableDeletion: true
    editable: false
    options:
      path: /var/lib/grafana/dashboards/llm-access-gateway
