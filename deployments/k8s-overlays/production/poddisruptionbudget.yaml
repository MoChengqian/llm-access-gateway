apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: llm-access-gateway
  namespace: llm-access-gateway
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: llm-access-gateway
